@model PlainTextModel

<link href="~/css/Cards/PlainText/style10.css" rel="stylesheet" />

<div id="plainText_@Model.CardNo" class="card-plainText" data-style="10" data-aos="fade-up">
    <div class="title-sub">@Html.Raw(Model.Description)</div>
    <div class="title-main">@Model.Title</div>
</div>